import type { RequestHandler } from 'express';
/** Stricter limit for auth endpoints (login, register, password reset). */
export declare function getAuthLimiter(): RequestHandler;
//# sourceMappingURL=rateLimiter.d.ts.map